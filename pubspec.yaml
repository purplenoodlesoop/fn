name: async_effects
description: Composable, testable async effects with zone-based dependency injection and no service locators.
version: 1.0.0
repository: https://github.com/purplenoodlesoop/fn

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  pure: ^0.3.0
  meta: ^1.17.0
  stream_transform: ^2.1.1
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
